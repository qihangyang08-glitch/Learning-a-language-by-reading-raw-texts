{
  "name": "jareader-translation-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker proxy for JaReader translation API",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250701.0",
    "typescript": "~5.0.0",
    "wrangler": "^4.0.0"
  }
}
